export interface Contact {
    id: number;
    firstName: string;
    lastName: string;
    phone: string;
}

export interface NewContact {
    firstName: string;
    lastName: string;
    phone: string;
}